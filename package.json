{
  "name": "root",
  "version": "1.0.0",
  "scripts": {
    "start": "lerna run start --parallel --scope server --scope client",
    "postinstall": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
